# 是否开启debug
DEBUG=0

# 系统服务地址，通常为项目的基础访问地址
SYSTEM_API_URL=http://your-project-url.com

# 加密配置：用于对称加密的数据密钥（16 字符）
SYSTEM_AES_KEY=your-16-char-key

# 加密配置：用于对称加密的初始向量（16 字符）
SYSTEM_AES_IV=your-16-char-iv

# 系统主密钥，推荐 32 位随机字符串，用于签名或其他安全相关用途
SYSTEM_KEY=your-32-char-secret-key

# 项目监听的端口号，例如 8080、9000 等
LISTEN=8080

# Redis 连接配置（缓存/队列用）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
# Redis 密码，如无则留空
REDIS_PASSWORD=

# MySQL 数据库连接配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_CHARSET=utf8mb4
DB_COLLATION=utf8mb4_general_ci